---
# defaults file for sudoers

# some default vars
sudoers_dir_path: "/etc/sudoers.d/"

# empty arrays for sudo-definitions
sudoers_all: []
sudoers_group: []
sudoers_host: []

file_state: "{{ item.state | default('present') }}"
